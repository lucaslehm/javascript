// aula 41 - Exercicios

// escreva uma função que recebe 2 numeros e retorne o maior deles

function numeroMaior(num1, num2) {
return num1 > num2 ? num1 : num2
}

const numMaior = (num1, num2) => num1 > num2 ? num1 : num2;

console.log(numMaior(4, 7))